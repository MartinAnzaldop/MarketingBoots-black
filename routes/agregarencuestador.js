//Rutas para agregar encuestador
const express = require('express');
const router = express.Router();

//api/productos
router.post('/', () => {
    console.log('creando usuario encuestador..');
})

module.exports = router;